name: x86-windows

on:
  workflow_dispatch:
  push:
    branches: [ "master", "dev" ]

env:
  BUILD_TYPE: Release

jobs:
  skip_check:
    continue-on-error: false
    runs-on: ubuntu-22.04
    outputs:
      should_skip: ${{ steps.skip_check.outputs.should_skip }}
    steps:
    - id: skip_check
      uses: fkirc/skip-duplicate-actions@v5
      with:
        concurrent_skipping: 'same_content'
        cancel_others: 'true'
        skip_after_successful_duplicate: 'true'
        paths_ignore: '["**/README.md", "**/docs/**", "**/LICENSE.txt" ]'
        do_not_skip: '["workflow_dispatch", "schedule"]'

  build:
    needs: skip_check
    if: needs.skip_check.outputs.should_skip != 'true'

    runs-on: windows-2022

    steps:
    - uses: actions/checkout@v3
      with:
        submodules: recursive

    - uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: x64

    - name: Configure CMake
      shell: bash
      run: CC=clang CXX=clang++ ./scripts/cmake_configure.sh

    - name: Build
      shell: bash
      run: ninja -C build

    - name: Copy files 
      shell: bash
      run: ./toolchains/windows/create_package.sh

    - name: Upload files 
      uses: actions/upload-artifact@v3
      with:
        name: torrent_renamer_cpp_windows_x64
        path: ${{github.workspace}}/torrent_renamer_cpp_windows_x64
